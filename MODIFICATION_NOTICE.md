# Modification Notice

Modification date: 2026-08-26.

Myuna retains the reviewed AstrBot response pipeline. The corresponding-source offer is now exposed through the local `/source` command instead of being appended to every ordinary reply. The accepted AstrBot image, base, config, manifest, and canonical OCI archive identities are unchanged.

This refresh retains the owner-runtime import closure and current-message-only hybrid request correction, and adds the matching Core HTTP schema dispatch for the existing episodic runtime context. The parser recognizes that existing schema before the established runtime validator; malformed and unknown release-bound schemas remain fail-closed. Prior raw/history authority remains solely inside the validated context, and non-hybrid short-term history behavior is unchanged. The change introduces no provider, endpoint, database, store, service, or additional runtime route.

The `/source` branch is handled locally after the established private-owner ingress checks and does not call the gateway, model, memory, or tools. The source snapshots and runtime manifest identify every changed source generation. No provider credential, private data, database row, message, log, or runtime secret is included.
