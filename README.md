# Myuna AstrBot Phase F Corresponding Source

This public repository provides the no-charge corresponding source for the reviewed Myuna AstrBot Phase F image and the selected owner-private-memory runtime.

The immutable AstrBot image binding is unchanged. The 2026-08-26 refresh binds the Core HTTP episodic-runtime parser repair, Deploy current-message-only owner-runtime repair, and runtime release recorded in `SOURCE_MANIFEST.json`.

Contents include complete exact Git snapshots for AstrBot, Myuna Core, and Myuna Deploy; the bytecode-stripped source runtime base; the sealed runtime manifest; deterministic build instructions; hashes; license; and modification notice. No credential, private message/media, database row, provider payload, raw log, or private configuration value is included.

See `BUILD_INSTALL_RUN.md`, `MODIFICATION_NOTICE.md`, `FILES.sha256`, and `PACKAGE.sha256`.
